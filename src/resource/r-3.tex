\documentclass{ctexart}
\usepackage{tikz}
\usepackage[top=0in,bottom=0in,right=0in,left=0in]{geometry}
\usepackage[active,tightpage]{preview}
\PreviewEnvironment{tikzpicture}
\setlength\PreviewBorder{0pt}%
\usetikzlibrary{shapes,intersections,patterns}
\usetikzlibrary{calc}
\usetikzlibrary{arrows.meta,decorations.pathmorphing,backgrounds,positioning,fit,petri}

%\input{ctex-fontset-custom.def}

\begin{document}
\begin{tikzpicture}[
    >={Latex[width=2mm,length=2mm]},
    node distance=1.5cm,
    every node/.style={fill=white},
    align=center,
    % Specifications for style of nodes:
    base/.style = {
      rectangle,
      rounded corners,
      draw=black,
      minimum width=3cm,
      minimum height=1cm,
      text centered
    },
    remoteSet/.style = {
      base,
      fill=blue!30
    },
    startstop/.style = {
      base,
      fill=red!30
    },
    clientSet/.style = {
      base,
      fill=green!30
    },
    hardwareSet/.style = {
      base,
      minimum width=2.5cm,
      fill=orange!15,
    },
  ]
  % Specification of nodes (position, etc.)
  \node (client1)         [clientSet]                                         {电源监控系统};
  \node (hardware1)       [hardwareSet, below=1cm of client1, xshift=-45mm]       {环境探针};
  \node (hardware2)       [hardwareSet, below=1cm of client1, xshift=-15mm]           {UPS};
  \node (hardware3)       [hardwareSet, below=1cm of client1, xshift=15mm]        {开关电源};
  \node (hardware4)       [hardwareSet, below=1cm of client1, xshift=45mm]        {蓄电池组};
  % Specification of lines between nodes specified above
  % with aditional nodes for description
  \draw[->]           (hardware1) -- (client1);
  \draw[->]           (hardware2) -- (client1);
  \draw[->]           (hardware3) -- (client1);
  \draw[->]           (hardware4) -- (client1);
%  \draw[->] (onPauseBlock.east) -- ++(2.6,0) -- ++(0,2) -- ++(0,2) --     node[xshift=1.2cm,yshift=-1.5cm, text width=2.5cm]     {The activity comes to the foreground}(onResumeBlock.east);
\end{tikzpicture}
\end{document}
